mutation PostChatMessage($room: String!, $message: String!) {
  postMessage(room: $room, message: $message) {
    message {
      room {
        messages {
          id
          body
          author {
            screenname
          }
        }
      }
    }
    errors
  }
}
